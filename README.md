# project_financial_timeseries
The implementation of the project in the course Financial Time Series TMS088

Step by step:

1. clone repo 
2. create venv
3. run "pip install -r requirements.txt"    # basic dependencies
4. run "pip install -e ."                   # package handling

